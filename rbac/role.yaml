apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: k8suser-role
  namespace: roboshop
rules:
  - apiGroups: [""]
    resources:
      - pods
      - services
    verbs:
      - get
      - list
      - watch
